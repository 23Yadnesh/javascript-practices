// primitive & non-primitive

// primitive = number, string, boolearn, null, undefine, symbol, bigInt

const score = 100
const scoreValue = 100.3

const loggedIn = false
const outsideTemp = null
let useremail;
 
const id = symbol ('123')
const anotherId = symbol ('123')

console.log(id===anotherId);

// non primitive = arrey, Object, functions

// arrey is writen in square bracket  const hero = [ ironman, spiderman, hulk]

// object is writen in curly bracket 
// let myobj = { name : "yadnesh", 
// age : 24, }

// Way of writing function  
// Const myFunction = function (){ 
//  console.log ( "hello yadnesh"); }




// there are two types of memory storing process Stack and Heap
// stack is used by primitive type & Heap is used by non primitive type.